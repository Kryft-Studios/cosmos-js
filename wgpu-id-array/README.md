# wgpu-id-array
### (recreation of `webgpu-ul`)

**WGPU ID Array** is a Array where:
- the syntax of a normal array in js can be used.
- Items can be deleted.
- The CPU Index doesn't match with the GPU Index.
- Items can be overwritten
- All items follow a struct.